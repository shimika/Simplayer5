<UserControl x:Class="Simplayer5.IndexerButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="36" d:DesignWidth="36"
             VerticalAlignment="Center" DataContext="{Binding RelativeSource={RelativeSource Self}}"
             >
    
    <Grid Width="36" Height="36" Background="LightGray">
        <Button Width="36" Height="36" Click="Button_Click" Background="{DynamicResource sColor}" Style="{DynamicResource FlatButton}" Visibility="{Binding Visible}"/>
        <TextBlock Text="{Binding Header}" HorizontalAlignment="Center" Foreground="White" FontSize="14" Margin="4,2,0,0"/>
    </Grid>
</UserControl>
